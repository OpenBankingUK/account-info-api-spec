OBReadParty1:
  type: object
  properties:
    Data:
      type: object
      properties:
        Party:
          $ref: '#/definitions/OBParty1'
      additionalProperties: false
    Links:
      $ref: '#/definitions/Links'
    Meta:
      $ref: '#/definitions/Meta'
  required:
    - Data
    - Links
    - Meta
  additionalProperties: false
