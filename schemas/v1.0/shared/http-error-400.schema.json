{
  "title": "HTTP 400 Error",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": [ "400" ]
    },
    "title": {
      "type": "string",
      "enum": [ "BadRequest" ]
    },
    "description": {
      "type": "string",
      "enum": [ "The request could not be understood by the server due to malformed syntax. The client SHOULD NOT repeat the request without modifications." ]
    }
  },
  "required": [
    "status",
    "title",
    "description"
  ],
  "additionalProperties": false
}
