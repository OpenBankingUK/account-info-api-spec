#HOW TO use definitions:
#Declare here using standard syntax e.g.
# CommonSchema:
#  $ref: ../../../../compiled/schemas/v0/payments/simple_cancel.delete.200.response.schema.yaml

# You can then refer to common definitions using this syntax
# relative path to index.yaml where the definitions file gets loaded
# $ref: '../../../index.yaml#/definitions/SimpleCancelDelete200ResponseSchema'
# this works only in swagger-parser@beta (v4.0.0) version which uses latest json-schema-ref-parser 3.5.2
